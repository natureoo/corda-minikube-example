kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-pv-volume-notary
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/postgres/notary/data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pv-claim-notary
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-notary
spec:
  selector:
    matchLabels:
      app: postgres-notary
  template:
    metadata:
      labels:
        app: postgres-notary
    spec:
      #      volumes:
      #        - name: postgres-storage
      #          persistentVolumeClaim:
      #            claimName: postgres-pv-claim
      containers:
        - image: postgres
          name: postgres-notary
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-notary
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-notary
                  key: postgres_password
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: postgres-config-notary
                  key: postgres_db
          #            - name: PGDATA
          #              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
              name: postgres-notary
          volumeMounts:
            - name: postgres-persistent-storage-notary
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-persistent-storage-notary
          persistentVolumeClaim:
            claimName: postgres-pv-claim-notary
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-notary
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres-notary
